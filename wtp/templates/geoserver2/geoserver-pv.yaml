apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "wtp.name" . }}-geoserver-pv-2
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-pv-2
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}  
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  {{- if .Values.geoserver2.volume.hostPath }}
  hostPath:
    path: {{ .Values.geoserver2.volume.hostPath }}
    type: DirectoryOrCreate
  {{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "wtp.name" . }}-geoserver-svc-2
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-svc-2
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.geoserver2.service.type }}
  ports:
    - port: {{ .Values.geoserver2.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-dpl-2
    app.kubernetes.io/instance: {{ .Release.Name }}

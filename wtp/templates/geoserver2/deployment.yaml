apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "wtp.name" . }}-geoserver-dpl-2
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-dpl-2
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  {{- if not .Values.geoserver2.autoscaling.enabled }}
  replicas: {{ .Values.geoserver2.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-dpl-2
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      {{- with .Values.geoserver2.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        helm.sh/chart: {{ include "wtp.chart" . }}
        app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-dpl-2
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
    spec:
      containers:
        - name: {{ .Chart.Name }}-geoserver-2
          image: "{{ .Values.geoserver2.image.repository }}:{{ .Values.geoserver2.image.tag }}"
          imagePullPolicy: {{ .Values.geoserver2.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - mountPath: "/opt/geoserver/data_dir"
              subPath: "data_dir"
              name: geoserver-volume-2
            - mountPath: "/opt/geoserver/raster_dir"
              subPath: "raster_dir"
              name: geoserver-volume-raster-2
          {{- with .Values.geoserver2.envs }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}       
      dnsPolicy: ClusterFirst
      dnsConfig:
        nameservers:
          - 192.168.12.13
          - 8.8.8.8
          - 1.1.1.1             
      volumes:
        - name: geoserver-volume-2
          persistentVolumeClaim:
            claimName: {{ include "wtp.name" . }}-geoserver-pv-claim-2
        - name: geoserver-volume-raster-2
          persistentVolumeClaim:
            claimName: {{ include "wtp.name" . }}-geoserver-raster-pv-claim-2
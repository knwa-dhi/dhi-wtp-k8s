apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "wtp.name" . }}-geoserver-raster-pv-2
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-geoserver-raster-pv-2
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}  
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  {{- if .Values.geoserver2.volume.rasterHostPath }}
  hostPath:
    path: {{ .Values.geoserver2.volume.rasterHostPath }}
    type: DirectoryOrCreate
  {{- end }}
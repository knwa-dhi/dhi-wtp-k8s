apiVersion: v1
kind: Service
metadata:
  name: {{ include "wtp.name" . }}-frontend-svc
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-frontend-svc
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    {{- if .Values.frontend.service.ports }}
      {{- range .Values.frontend.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}    
      targetPort: {{ .targetport | default "http" }}
      {{- end }}
    {{- else }}
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
    {{- end }}
  selector:
    app.kubernetes.io/name: {{ include "wtp.name" . }}-frontend-dpl
    app.kubernetes.io/instance: {{ .Release.Name }}

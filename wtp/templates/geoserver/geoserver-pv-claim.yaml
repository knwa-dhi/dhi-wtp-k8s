apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "wtp.name" . }}-geoserver-pv-claim
  labels:
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-pv-claim
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}  
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: {{ include "wtp.name" . }}-geoserver-pv
#   storageClassName: ""
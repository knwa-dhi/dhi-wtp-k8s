apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "wtp.name" . }}-ds-postgres-pv-volume
  labels:
    type: local
    helm.sh/chart: {{ include "wtp.chart" . }}
    app.kubernetes.io/name: {{ include "wtp.name" . }}-ds-postgres-pv-volume
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    # path: "/mnt/dsapidb-data"
    path: "/water-status-data/dsapidb-data"